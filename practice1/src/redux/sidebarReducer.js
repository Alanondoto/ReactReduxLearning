let initialState = {
  friends: [
    {id: 1, name: 'Alex'},
    {id: 2, name: 'Masha'},
    {id: 3, name: 'Ignat'}
  ]
};

const sidebarReducer = (state = initialState, action) => {

  return state;
}

export default sidebarReducer;